# Copy to .env (git-ignored). Same project and anon key as the web app's .env.
# EXPO_PUBLIC_ variables are inlined into the app bundle — never put a secret here.
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
